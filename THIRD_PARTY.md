# Third-party materials

The Linux kernel is GPL-2.0-only. Device-tree bindings and upstream patches
retain their original licenses and notices.

The Qualcomm WCN7850 firmware files are included only when supplied by the
local build input. Their hashes are recorded so a downstream project can
replace them with an independently obtained, license-compliant copy. Check
the vendor terms before redistribution.
