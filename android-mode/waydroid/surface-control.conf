# The host control daemon is intentionally the only host interface exposed to
# the Android container. No Wi-Fi, Bluetooth, DRM, or PCI device is passed in.
lxc.mount.entry = /run/surface-android-control/control.sock run/surface-control.sock none bind,create=file,optional 0 0
